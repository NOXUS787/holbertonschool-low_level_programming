#include "main.h"

/**
 * main - Prints _putchar followed by a new line
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
int i = 0 ;
char str[]= "_putchar";

 while(str[i]!='\0')
   {
     _putchar(stre
 for(i = 0; str[i] != '\0'; i++)
   {
      
 _putchar(str[i]);

   }
 
 _putchar('n');

	return (0);
}
